class Product {
  final int id;
  final String name;
  final bool isActive;
  final DateTime createdDate;

  Product({
    required this.id,
    required this.name,
    required this.isActive,
    required this.createdDate,
  });
}
